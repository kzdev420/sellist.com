<% size ||= :mini %>
<% itemprop ||= nil %>

<% if image_url = image.try(:url, size)  %>
  <% if image_alt = image.try(:alt) %>
    <%= image_tag image_url, alt: image_alt, itemprop: itemprop, class: "rounded mr-3", height: "64" %>
  <% else %>
    <%= image_tag image_url, itemprop: itemprop, class: "rounded mr-3", height: "64" %>
  <% end %>
<% else %>
  <span class="image-placeholder <%= size %>"></span>
<% end %>
